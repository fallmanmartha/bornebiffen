---
// Henter parametrene fra Astro.props
const { htmlheading = "h1", size = "xlarge", font = "heading", color = "light", weight = "bold" } = Astro.props;
const Htmlheading = htmlheading;

//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige størrelser.
const sizeClasses = {
  xlarge: "text-mobile-h1 lg:text-computer-h1",
  large: "text-mobile-h2 lg:text-computer-h2",
  medium: "text-mobile-h3 lg:text-computer-h3",
  small: "text-mobile-h4 lg:text-computer-h4",
  xsmall: "text-mobile-h5 lg:text-computer-h5",
  cta: "text-cta-small lg:text-cta-large",
};

//Definer den styling (Tailwind classes), der gør sig gældende for de forskellige skrifttyper.
const fontClasses = {
  heading: "font-heading",
};

const colorClasses = {
  light: "main-text-primary",
  dark: "main-text-secondary",
};

const stylingClasses = {
  bold: "font-bold",
};
---

<Htmlheading class:list={[colorClasses[color], sizeClasses[size], stylingClasses[weight], fontClasses[font]]}><slot /></Htmlheading>
